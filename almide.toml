[package]
name = "fizz_memory_l3_summarizer"
version = "0.1.0"
description = "Fizz memory L3 — roll a per-user summary via the LLM once new events cross a threshold"

[dependencies]
fizz_protocol = { git = "https://github.com/aiviecast/fizz-protocol", tag = "v0.2.2" }
fizz_llm_client = { git = "https://github.com/aiviecast/fizz-llm-client", tag = "v0.2.1" }
